apiVersion: kustomize.config.k8s.io/v1beta1 # Kustomize API version.
kind: Kustomization # Declares this file as a Kustomize manifest.

# Include all chaos experiments so they can be managed from chaos-mesh root.
resources:
  - experiments # Child kustomization with all experiment manifests.
